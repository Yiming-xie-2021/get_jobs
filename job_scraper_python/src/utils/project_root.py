import os

# PROJECT_ROOT is the absolute path to the 'job_scraper_python' directory
PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..'))

# DATA_DIR is the absolute path to the 'data' directory inside 'job_scraper_python'
DATA_DIR = os.path.join(PROJECT_ROOT, 'data')
